To run this program:

- change config_example.txt to config.txt, and change parameters as needed (DEBUG=False to record real data, change sample and cooldown name (changes saved file name) and folder to save to.

- add g2python folder to the PYTHONPATH (under tools, PYTHONPATH manager in Spyder, but you have to click "synchronize" after adding it)